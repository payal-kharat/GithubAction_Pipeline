name : "hello world workflow"

on:
  push:
    branches:
      -master
      -production


jobs:
  hello_world:
    runs-on : ubuntu_latest
    steps:
      - name: "print hello world"
        run: echo "hello world"


